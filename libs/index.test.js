const { judgePet } = require('./SOCPageant');

/* Task 5 - write three tests to ensure the 'judgePet' function is working as you would want it to (i.e. returns to correct string).*/

test('', () => {});
